/*
    Bubble Sort

    passes through array

    when elements are out of order, swap them

    when no swaps occur in a full pass, stop


    best = O(n)
    avg = O(n^2)   
    worst = O(n^2)
*/

#include <stdio.h>
#include <stdlib.h>




int main()
{




    return 0;
}